namespace HuaweiARUnitySDK
{   
    /**
     * \if english
     * @brief Enumeration of camera lens facing.
     * \else
     * @brief 相机镜头的枚举。
     * \endif
     */
    public enum ARConfigCameraLensFacing
    {
        /**
         * \if english
         * Rear Camera.
         * \else
         * 后置镜头。
         * \endif
         */
        REAR=0,
        /**
         * \if english
         * Front Camera.
         * \else
         * 前置镜头。
         * \endif
         */
        FRONT = 1,
    }
}
