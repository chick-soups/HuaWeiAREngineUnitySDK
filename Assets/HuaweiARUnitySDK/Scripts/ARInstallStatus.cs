namespace HuaweiARUnitySDK
{
    /**
     * \if english
     * @brief The install status of AR Engine.
     * \else
     * @brief AR Engine的安装状态。
     * \endif
     */
    public enum ARInstallStatus
    {
        /**
         * \if english
         * Already installed.
         * \else
         * 已安装。
         * \endif
         */
        INSTALLED = 0,
        /**
         * \if english
         * Already request installation.
         * \else
         * 已经请求安装。
         * \endif
         */
        INSTALL_REQUESTED = 1,
    }
}
