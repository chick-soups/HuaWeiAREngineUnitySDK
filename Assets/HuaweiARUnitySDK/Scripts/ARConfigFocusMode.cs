namespace HuaweiARUnitySDK
{
    /**
     * \if english
     * @brief Enumeration of focus mode.
     * \else
     * @brief 对焦模式。
     * \endif
     */
    public enum ARConfigFocusMode
    {
        /**
         * \if english
         * @brief Fixed focus.
         * \else
         * @brief 固定对焦到无穷远。
         * \endif
         */
        FIXED_FOCUS = 0,
        /**
         * \if english
         * @brief Auto focus.
         * \else
         * @brief 自动对焦。
         * \endif
         */
        AUTO_FOCUS = 1,
    }
}